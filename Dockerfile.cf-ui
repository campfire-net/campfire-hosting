FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /cf-ui ./cmd/cf-ui

FROM alpine:3.19
RUN apk add --no-cache ca-certificates
COPY --from=build /cf-ui /usr/local/bin/cf-ui
EXPOSE 8080
ENTRYPOINT ["cf-ui"]
